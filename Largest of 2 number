#include<stdio.h>
void main() {
int a,b;
printf("Enter the value of a and b\n");
scanf("%d%d",&a,&b);
if(a>b)
printf("the value of a is largest=%d\n",a);
else
printf("The value of b is largest=%d\n",b);
}
