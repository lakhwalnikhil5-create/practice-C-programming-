#include<stdio.h>
int main(){
int num;
printf("Enter the value of num\n");
scanf("%d",&num);
if(num>0)
printf("The number is positive=%d",num);
else
printf("the number is negative=%d",num);
return 0;
}
